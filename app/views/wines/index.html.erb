<h1>Wines<h1>
	<p>Available as <%= formatted_time(@available_at) %></p>
	<%= page_entries_info @wines %>
	<ol>
		<% @wines.each do |wine| %>
		<li><%= link_to(wine.name, wine) %>, <%= wine.varietal %> by <%= wine.winery %></li>
		<% end %>
	</ol>
	<%= paginate @wines %>